language: java
sudo: false
jdk:
  #- openjdk8
  - openjdk11

cache:
  directories:
  - "$HOME/.m2"

addons:
  apt:
    packages:
    - libcppunit-dev
    
  sonarcloud:
    organization: "ceciliacal"
    token: "8b7bb333d9d20fd2c16f7d5f92892c1a951cf56b"  
    

install: mvn clean install -q -DskipTests=true

script: mvn clean verify  sonar:sonar spotbugs:check checkstyle:check -Pfull-build

branches:
  only:
  - master
  - branch-3.5
  - branch-3.4